import Reng from '../../../local/index';

@Reng.Component({
  selector: 'Default',
  template:
    `<div id="default">Hello from default</div>
    `
})
export default class Default extends Reng.View {
}
